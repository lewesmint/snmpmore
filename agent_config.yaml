# SNMP Agent Configuration
# List the MIBs to load (without extension)
# Example:
# mibs:
# - SNMPv2-MIB  # Standard system group (sysDescr, sysContact, etc.)
# - IF-MIB
# - HOST-RESOURCES-MIB
# - HOST-RESOURCES-TYPES
# - CISCO-ALARM-MIB

mibs:
  - SNMPv2-MIB  # Standard system group (sysDescr, sysContact, etc.)
  - IF-MIB
  - HOST-RESOURCES-MIB
  - HOST-RESOURCES-TYPES
  - CISCO-ALARM-MIB
